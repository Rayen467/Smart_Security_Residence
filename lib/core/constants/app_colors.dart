import 'package:flutter/material.dart';

class AppColors {
  // Primary colors
  static const Color primary = Color(0xFF0F172A); // Navy gelap
  static const Color primaryLight = Color(0xFF1E293B);
  static const Color primaryDark = Color(0xFF020617);

  // Security accent colors
  static const Color accent = Color(0xFF2563EB); // Biru security
  static const Color success = Color(0xFF16A34A); // Hijau aman
  static const Color warning = Color(0xFFF59E0B); // Kuning peringatan
  static const Color danger = Color(0xFFDC2626); // Merah SOS/darurat
  static const Color info = Color(0xFF0284C7);

  // Background
  static const Color background = Color(0xFFF8FAFC);
  static const Color surface = Colors.white;
  static const Color card = Colors.white;

  // Text
  static const Color textPrimary = Color(0xFF0F172A);
  static const Color textSecondary = Color(0xFF64748B);
  static const Color textHint = Color(0xFF94A3B8);

  // Border & divider
  static const Color border = Color(0xFFE2E8F0);
  static const Color divider = Color(0xFFE5E7EB);

  // Status
  static const Color online = Color(0xFF22C55E);
  static const Color offline = Color(0xFF64748B);
  static const Color motionDetected = Color(0xFFEF4444);
}
